import { ScaleFilterDirective } from './scale-filter.directive';

describe('ScaleFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleFilterDirective();
    expect(directive).toBeTruthy();
  });
});
